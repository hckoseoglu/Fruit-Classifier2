---
applyTo: "**"
---

Don't make any changes that weren't requested by the user including formats and fixes not explicitly asked for.
Only make the changes that were explicitly requested by the user.
If the user requests multiple changes, make all of them in a single response.
Dont put comments that directly addressing me (the user) like the following: "(always do this before PCA)" in the code.
